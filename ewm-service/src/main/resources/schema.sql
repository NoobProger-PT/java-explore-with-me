DROP TABLE IF EXISTS users CASCADE;
DROP TABLE IF EXISTS events CASCADE;
DROP TABLE IF EXISTS categories CASCADE;
DROP TABLE IF EXISTS participations CASCADE;
DROP TABLE IF EXISTS compilations CASCADE;
DROP TABLE IF EXISTS compilations_events CASCADE;
DROP TABLE IF EXISTS comments CASCADE;
DROP TABLE IF EXISTS comments_likes CASCADE;
DROP TABLE IF EXISTS comments_dislikes CASCADE;

CREATE TABLE IF NOT EXISTS users (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    name VARCHAR NOT NULL,
    email VARCHAR NOT NULL,
    CONSTRAINT user_email UNIQUE (email)
);

CREATE TABLE IF NOT EXISTS compilations (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    pinned BOOLEAN,
    title  VARCHAR
);

CREATE TABLE IF NOT EXISTS categories (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    name VARCHAR NOT NULL,
    CONSTRAINT category_name UNIQUE (name)
);

CREATE TABLE IF NOT EXISTS events (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    annotation VARCHAR(2000) NOT NULL,
    category_id VARCHAR NOT NULL,
    description VARCHAR(7000),
    event_Date TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    paid BOOLEAN,
    participant_Limit INTEGER,
    request_Moderation BOOLEAN,
    title VARCHAR(120) NOT NULL,
    created_On TIMESTAMP WITHOUT TIME ZONE,
    published_On TIMESTAMP WITHOUT TIME ZONE,
    state VARCHAR,
    initiator_id BIGINT NOT NULL,
    lat FLOAT,
    lon FLOAT,
    CONSTRAINT initiator_id_fk foreign key (initiator_id) references users,
    CONSTRAINT category_id_fk foreign key (category_id) references categories
);

CREATE TABLE IF NOT EXISTS participation (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    created TIMESTAMP WITHOUT TIME ZONE,
    event_id  BIGINT NOT NULL,
    requester_id  BIGINT NOT NULL,
    status VARCHAR NOT NULL,
    CONSTRAINT requester_id_fk foreign key (requester_id) references users,
    CONSTRAINT event_id_fk foreign key (event_id) references events
);

CREATE TABLE IF NOT EXISTS compilations_events (
    events_id BIGINT,
    compilation_id BIGINT,
    CONSTRAINT compilations_events_pk primary key (events_id, compilation_id),
    CONSTRAINT events_id_fk foreign key (events_id) references events,
    CONSTRAINT compilation_id_fk foreign key (compilation_id) references compilations
);

CREATE TABLE IF NOT EXISTS comments (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    text VARCHAR(1000) NOT NULL,
    author_name VARCHAR NOT NULL,
    author_id BIGINT NOT NULL,
    event_id BIGINT NOT NULL,
    comment_date TIMESTAMP WITHOUT TIME ZONE NOT NULL,
    CONSTRAINT author_id_fk foreign key (author_id) references users,
    CONSTRAINT id_event_fk foreign key (event_id) references events
);

CREATE TABLE IF NOT EXISTS comments_likes (
    likes_id BIGINT,
    comment_id BIGINT,
    CONSTRAINT comments_users_pk primary key (likes_id, comment_id),
    CONSTRAINT fk_user_id foreign key (likes_id) references users,
    CONSTRAINT fk_comment_id foreign key (comment_id) references comments
);

CREATE TABLE IF NOT EXISTS comments_dislikes (
    dislikes_id BIGINT,
    comment_id BIGINT,
    CONSTRAINT comments_dislikes_pk primary key (dislikes_id, comment_id),
    CONSTRAINT fk_users_id foreign key (dislikes_id) references users,
    CONSTRAINT fk_comments_id foreign key (comment_id) references comments
);